{
  "name": "admin",
  "password": "admin",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@admin-interface/core": "^0.1.30",
    "@admin-interface/express": "^0.1.32"
  }
}
